const home = require('../route/home');
const message = require('../route/message');
const user = require('../route/user');
const auth = require('../route/auth');
module.exports = { home, message, user, auth };
